/** Maximum classroom height. */
export const FLOOR_HEIGHT = 5;

/** Multiplier applied to non-highlighted objects' opacity when the
 *  scene is in "dim" mode. 0.05 keeps spatial context faintly visible. */
export const DIM_OPACITY = 0.05;
